<div class="container">
  <div class="mt-3 p-4">
    <%= simple_form_for(@cocktail) do |f| %>
      <%= f.input :name %>
      <%= f.submit %>
    <% end %>
  </div>
</div>

<%= link_to 'See all cocktails', cocktails_path(@cocktail) %>